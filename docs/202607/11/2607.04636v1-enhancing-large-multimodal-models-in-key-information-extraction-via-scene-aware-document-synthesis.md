---
title: Enhancing Large Multimodal Models in Key Information Extraction via Scene-Aware Document Synthesis
title_zh: 通过场景感知的文档合成增强关键信息提取中的大型多模态模型
authors: "Zhipeng Xu, Zulong Chen, Qing Liu, Junhao Ji, Jinxin Hu, Yipeng Yu, Jianqiang Wan, Jun Tang, Zhao Li"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04636v1"
tags: ["query:self-evolve"]
score: 9.0
evidence: 通过错误驱动生成从失败案例合成难负样本，实现信息抽取的自我改进
tldr: 关键信息抽取将富文本文档转为结构化数据，但本地小模型缺乏足够监督。本文提出场景感知文档合成框架 SAYRE，无需模板即可从少量样例生成训练数据，并引入错误驱动生成提升困难样本覆盖。在受限与开放类别 KIE 任务上，SAYRE 显著提升 Qwen3-VL 等端侧多模态大模型性能，数据扩展实验表明合成数据越多效果越好，尤其对小模型和开放类别抽取。该方法为实用化多模态 KIE 提供了高效数据驱动方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04636v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 807, \"height\": 901, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04636v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 774, \"height\": 605, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04636v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1538, \"height\": 917, \"label\": \"Table\"}]"
motivation: 实用 KIE 需要部署本地小模型，但其训练缺乏高质量标注，手工模板成本高且覆盖场景有限。
method: SAYRE 从少量样例文档中捕获类别内容与布局模式，自动合成文档-模式-标注三元组，并基于真实错误案例生成困难训练样本。
result: 在受限与开放类别 KIE 基准上，SAYRE 持续提升 Qwen3-VL 性能，取得端侧最佳结果，并在密集表格、证件和合同字段错误率上显著降低。
conclusion: 场景感知合成是提升实用多模态 KIE 的数据中心有效途径，未来可结合更复杂视觉理解和动态布局生成。
---

## 摘要
关键信息提取（KIE）将视觉丰富的文档转换为结构化数据，但实际部署仍面临挑战：强大的性能往往依赖于昂贵的服务器端大型多模态模型（LMM），而紧凑的本地可部署模型缺乏足够的KIE监督。我们提出了SAYRE，一个场景感知的文档合成框架，用于生成可扩展的KIE训练数据，而无需手工设计模板。给定少量示例文档，SAYRE捕获特定类别的内部模式与布局惯例，合成文档-模式-标注三元组。它进一步引入了错误驱动的生成，将现实世界的失败案例扩展为困难训练示例，同时保留其结构模式。在受限和开放类别KIE上的实验表明，SAYRE持续改进了Qwen3-VL骨干模型，并在设备端LMM中取得了最强的整体性能。数据规模实验显示，随着更多合成数据的引入，整体呈上升趋势，对于较小模型和开放类别提取尤为明显。错误分析进一步表明，合成训练通过改善对密集表格、商业标识符和合同条款的模式感知提取，减少了字段级错误。这些结果确立了场景感知合成为一种有效的数据中心方法，用于改进实际的多模态KIE。

## Abstract
Key Information Extraction (KIE) converts visually rich documents into structured data, but practical deployment remains challenging: strong performance often relies on costly on-server Large Multimodal Models (LMMs), while compact locally deployable models lack sufficient KIE supervision. We present SAYRE, a scene-aware document synthesis framework for generating scalable KIE training data without hand-crafted template design. Given a few exemplar documents, SAYRE captures category-specific content patterns and layout conventions to synthesize document-schema-annotation triples. It further introduces error-driven generation, which expands real-world failure cases into hard training examples while preserving their structural patterns. Experiments on constrained- and open-category KIE show that SAYRE consistently improves Qwen3-VL backbones and achieves the strongest overall performance among on-device LMMs. Data scaling experiments show an overall upward trend as more synthesized data is introduced, especially for smaller models and open-category extraction. Error analysis further shows that synthesized training reduces field-level errors by improving schema-aware extraction over dense tables, business identifiers, and contract clauses. These results establish scene-aware synthesis as an effective data-centric approach for improving practical multimodal KIE.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：关键信息提取（KIE）将视觉丰富的文档自动转为结构化字段，是发票处理、合同管理等下游应用的基础环节。然而，实际部署面临两难困境：
  - 高性能依赖服务器端大型多模态模型（LMM），但推理成本高、延迟大，不适合企业本地化部署。
  - 可本地部署的紧凑型 LMM 缺乏足够的 KIE 训练监督，尤其在多变的布局、领域特定模式与长尾字段配置下表现不足。
- **根本瓶颈**：高质量 KIE 训练数据的规模化获取困难，现有合成方法依赖手工模板或简单内容替换，难以扩展新类别，且缺乏对真实失败案例的覆盖。
- **研究意义**：提出一种数据驱动路径，通过场景感知的文档合成，为小模型提供丰富且有针对性的训练数据，缩小本地模型与大型云模型之间的能力差距。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：SAYRE 是一个无手工模板的文档合成框架，结合通用数据生成与错误驱动生成，输出文档图像、查询模式与结构化标注的完整三元组。
- **通用数据生成**（第 3.1 节）：
  - 输入极少量的类别示例文档 $\{x_i\}_{i=1}^n$ 以及类别标识 $I_c$ 和角色卡 $P$。
  - 多智能体系统协同工作：
    - 主题生成：$V_t$ 基于 $I_c$ 与 $P$ 产生新主题 $t$。
    - 感知：内容感知 $V_C$ 产生内容描述 $C$（常见字段、语义依赖），布局感知 $V_L$ 产生布局描述 $L$（页面尺寸、层次结构、空间分布）。
    - 内容生成：$V_J$ 依据 $C, t, P$ 生成结构化输出 $y$，并由 $y$ 推导出对应模式 $S$。
    - 文档生成：$V_D$ 结合 $y$ 与 $L$ 生成 HTML 代码 $H_x$，最终渲染为文档图像 $x$。
  - 领域适配：可选模式引导提取 $V_A$ 将 $y$ 映射到特定下游模式 $S'$，得到 $y'$。
- **错误驱动数据生成**（第 3.2 节）：
  - 从模型测试与生产系统中收集真实失败案例，人工标注形成语料 $X_f$。
  - 将失败文档转为 HTML 模板 $H_{x'}$，提取文本块 $T$，与标签 $y'$ 建立映射 $M$。
  - 使用 LLM 对文本块重写为语义相似但内容完全不同的 $\tilde{T}$，用于去标识化。
  - 根据 $M$ 与 $\tilde{T}$ 更新标签 $\tilde{y}'$，将新文本回填至模板，渲染为新文档 $\tilde{x}'$。
  - 对于映射不可靠的字段，采用多模型投票重新标注。
- 最终合成数据还会加入真实的光学噪声模拟。

### 3. 实验设计：使用的数据集/场景、benchmark 和对比方法

- **Benchmark**：UniKIE，通用 KIE 基准，覆盖多种文档类型与异构字段定义，分为受限类别（Constrained-Category）和开放类别（Open-Category）两种评估设置。
- **对比基线**：
  - **设备端 LMM**（8B 左右）：MiniCPM‑V4.5-8B、InternVL3.5-8B、Qwen3-VL-2B 和 Qwen3-VL‑4B、Ministral‑3‑8B、MiMo‑VL‑7B‑RL、GLM‑4.1V‑9B。
  - **服务器端 LMM**（上界）：GPT‑4o、GPT‑5、Claude‑Sonnet‑4.5、Qwen‑VL‑Max、Qwen3‑VL‑Plus、Gemini‑3‑Pro。
- **评估指标**：字段级 F1 分数，要求提取值经标准化后与真值完全匹配。
- **骨干模型**：Qwen3-VL‑2B 和 Qwen3‑VL‑4B 作为基础模型，用 SAYRE 合成数据全量微调 15K 步。

### 4. 资源与算力

- 训练使用 **4 块 NVIDIA A800 GPU**，DeepSpeed ZeRO Stage 2 分布式全参数微调。
- 每设备批次大小 4，梯度累积 4 步，全局批次大小 64。
- 训练 15K 步，学习率 5e‑6，余弦调度，最大序列长度 8192，最大图像分辨率 1,605,632 像素，bfloat16 精度。
- 合成阶段使用 Qwen‑VL‑Max（感知布局/内容）、Qwen3‑Max（生成内容与 HTML）、Qwen3‑VL‑Plus（错误驱动模板生成），具体计算量未详细说明，但合成总数据量 1M 个实例。

### 5. 实验数量与充分性

论文进行了多维度实验，整体数量充足、覆盖全面：
- **主实验**：两个版本（SAYRE‑2B、SAYRE‑4B）在所有七个子类别（商业交易、公共服务、登记/记录、收据、表格、发票、合同）上对比 6 个设备端基线、6 个服务器端基线，评估受限与开放类别下的表现（表1）。
- **数据规模扩展实验**（图1）：分别在 0 K、2 K、4 K、… 14 K 训练步数下，评估受限、开放类别的 F1 以及平均增益，展示数据量增加对 2B 与 4B 模型的影响趋势。
- **错误分析**（图2）：对比 Qwen3‑VL‑4B 与 SAYRE‑4B 在六种解释性错误组上的字段级错误数量，并计算错误减少比例。
- 实验对比公平：均在相同 UniKIE 基准、同一评价指标下，与开源和商业模型比较，SAYRE 模型基于相同基础 Qwen3‑VL 骨干微调。

### 6. 论文的主要结论与发现

- **一致提升**：SAYRE 合成数据持续增强 Qwen3‑VL 各个规模的 KIE 性能，在设备端模型中取得最高平均成绩；4B 版本甚至接近一些大型服务器端系统性能。
- **开放类别更受益**：尤其在需要适应未见模式与字段定义的开放类别设置下，性能增益更大，表明合成数据提升了模型的场景泛化能力。
- **数据可扩展**：随着合成训练数据量增加，性能总体呈上升趋势，小模型（2B）获益更明显；数据规模收益在较难的开放类别上持续保持。
- **错误模式改善**：合成训练主要减少了密集表格的行项目字段错误、合同条款字段错误和商业标识符字段错误，错误总数下降 17.8%，虚警与漏报均有改善，说明模型获得了更好的模式对齐与布局结构化提取能力。

### 7. 优点：方法或实验设计上的亮点

- **免模板的场景感知合成**：从少量样例中自动学习内容与布局模式，避免手工模板设计，可灵活扩展到新文档类别。
- **错误驱动的难例扩充**：将真实生产中的失败案例反向转化为困难训练样本，覆盖了通用合成可能缺失的挑战场景，直接改善关键字段提取。
- **完整三元组生成**：同时输出文档图像、提取结构模式与字段标注，确保训练数据的完整性与一致性。
- **数据增强逼真度**：合成图像加入光学噪声，模拟真实采集条件，提高模型对实际退化图像的鲁棒性。
- **严谨的实验评估**：在统一基准上对比多种最新 LMM，包含受限与开放两种设定，并进行扩展性和错误分析实验，多角度验证了方法的有效性。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **手写内容支持有限**：当前合成框架尚不能可靠生成逼真的手写文本及其视觉变化，导致模型在混合印刷‑手写文档上性能降低。
- **合成质量依赖基座模型**：生成过程依赖 Qwen 系列模型进行感知、内容生成和 HTML 代码生成，其内在偏差可能会被传递给合成数据。
- **领域覆盖广度未充分探索**：主要实验基于 UniKIE 的若干类别，对更多样化的语言、文化背景或非结构化文档（如科学论文、法律长文档）的泛化能力未经测试。
- **错误驱动数据构建成本**：失败案例的收集和人工标注仍需一定投入，且映射缺失时的多模型投票也增加了计算开销。
- **训练数据量固定**：实验中合成数据总规模为 1M 实例，虽显示出向上趋势，但未能直接给出饱和点或成本效益最优规模。

（完）
