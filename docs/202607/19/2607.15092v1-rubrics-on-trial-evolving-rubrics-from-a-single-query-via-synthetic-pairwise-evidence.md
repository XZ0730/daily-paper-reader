---
title: "Rubrics on Trial: Evolving Rubrics from a Single Query via Synthetic Pairwise Evidence"
title_zh: 试炼中的评分标准：通过合成成对证据从单一查询演化评分标准
authors: "Haocheng Yang, Licheng Pan, Xiaoxi Li, Zhichao Chen, Zhiheng Zhang, Yuan Lu, Haoxuan Li, Hao Wang"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15092v1"
tags: ["query:self-evolve"]
score: 8.0
evidence: 框架通过合成证据从空集演化评分标准，是 NLP 评估中的自改进过程。
tldr: 在大型语言模型（LLM）的训练与评估中，查询特定的评分细则可提供结构化细粒度信号，但其可靠构建极具挑战。现有方法常依赖人工标注、偏好数据或采样回复，直接生成虽无需资源，但缺乏效用验证，易产生无用或误导性细则。为此，本文提出Rubrics on Trial框架，仅需查询输入，无需外部标注或模型训练，通过合成细则条件的响应对获取监督，从空集进化并验证每个候选细则，自动剔除无效项。在五个偏好基准套件上实验，该方法取得最优平均准确率，在七个评估集中的六个领先。证明其能有效生成判别性细则，为LLM提供可靠的无外部标注评估信号。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15092v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1425, \"height\": 1002, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15092v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1447, \"height\": 390, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15092v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1452, \"height\": 353, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15092v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1292, \"height\": 406, \"label\": \"Table\"}]"
motivation: 现有查询特定评分细则构建依赖外部资源或缺乏验证，导致细则无判别力或误导，需要纯查询驱动且能自验证的方法。
method: 提出Rubrics on Trial框架，从空集开始，利用合成细则条件的响应对提供监督，进化并验证候选项，筛除非判别性、过度具体和纯风格细则。
result: 在五个偏好基准数据集上，平均准确率最优，并在七个评估集中六个表现最佳，超越现有方法。
conclusion: Rubrics on Trial实现了无外部标注的查询特定细则自动构建，能生成高效判别细则，为LLM细粒度评估提供新思路。
---

## 摘要
评分标准为训练和评估大语言模型提供了结构化、细粒度的信号。然而，可靠的查询特定评分标准难以构建。现有方法通常从人工编写的评分标准、偏好数据或采样响应中获取监督信号。直接的查询到评分标准生成避免了这些资源，但无法显式检验一个看似合理的评分标准是否有用。这样的评分标准可能无法区分答案质量，奖励一种可有可无的风格，或惩罚一种有效的替代策略。我们引入了试炼中的评分标准（Rubrics on Trial），一个仅需查询的框架，它从空集开始演化出一组评分标准，无需外部注释或模型训练。它完全从合成的评分标准条件响应对中获取监督信号，并在添加每个提议的评分标准之前对其进行验证，从而筛除无区分力、过于具体和仅关乎风格的候选评分标准。在五个偏好基准套件上的实验证明了试炼中的评分标准的有效性，它取得了最佳平均准确率，并在七个评估集中的六个上领先。

## Abstract
Rubrics provide structured, fine-grained signals for training and evaluating large language models (LLMs). Yet reliable query-specific rubrics are difficult to construct. Existing approaches often derive supervision from human-written rubrics, preference data, or sampled responses. Direct query-to-rubric generation avoids these resources, but provides no explicit check that a plausible rubric is useful. Such a rubric may fail to distinguish answer quality, reward an optional style, or penalize a valid alternative strategy. We introduce Rubrics on Trial, a query-only framework that evolves a rubric set from an empty set without external annotations or model training. It derives supervision solely from synthetic rubric-conditioned response pairs and validates each proposed rubric before adding it, screening out non-discriminative, over-specific, and style-only candidate rubrics. Experiments across five preference benchmark suites demonstrate the effectiveness of Rubrics on Trial, which achieves the best average accuracy and leads on six of seven evaluation sets.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：评分标准为训练和评估大型语言模型提供细粒度的结构化信号，但可靠的、查询特定的评分标准极难自动构建。
- **现有方法不足**：
  - 依赖人工标注、偏好数据或采样响应的外部监督方法成本高、泛化受限。
  - 仅从查询直接生成的“查询唯一”方法虽避免外部资源，但缺乏显式检验，生成的评分标准可能**无区分力**、**奖励可选风格**或**排斥有效替代策略**，反而误导评判或引发奖励作弊。
- **核心问题**：如何在不使用任何外部注释、不训练模型的前提下，从空集逐步演化出一组**经过验证的、高质量**的查询特定评分标准。
- **整体含义**：提出一种“试炼”框架，借助合成评分标准条件响应对进行内部监督，让每个候选评分标准在加入前必须通过两道互补的成对比较测试，从而自动形成可靠且具有判别力的评分标准集合。

### 2. 论文提出的方法论
- **核心思想**：将评分标准的验证从抽象的标准集比较转移到**响应空间中的成对比较**，利用大语言模型在比较两个响应时更可靠的特性，通过双对设计筛选出真正有用的评分标准。
- **框架结构**：多角色智能体框架，包含**评分标准提议者**、**响应生成器**和**评分标准盲的成对判断器**，并由确定性控制器维护演化树、当前评分标准集和演化记忆。
- **树结构演化过程**（算法1）：
  - 从仅包含虚拟根节点的空树 \(T_0\) 和空评分标准集 \(R_0 = \emptyset\) 开始。
  - 每一步 \(t\)，提议者根据查询 \(q\)、当前活跃叶评分标准 \(R_t\) 和演化记忆 \(H_t\) 发出一个补丁 \(p_t\)：要么 `ADD(r)` 添加一条新原子标准，要么 `SPLIT(r_p → \{r_1, …, r_m\})` 拆分一个过于宽泛的标准为多个子标准。
  - 对每个候选标准 \(r\)，定义**试验背景** \(B_t\)（`ADD` 时为 \(R_t\)，`SPLIT` 时为 \(R_t \setminus \{r_p\}\)），随后构建两个互补的响应对进行验证。
- **合成响应对构建与验证**：
  - **本地编辑对**：首先生成同时满足 \(B_t\) 和 \(r\) 的强响应 \(a^+\)，再对其进行**最小化编辑**生成 \(a^-\)，使其仅违反 \(r\) 而继续满足 \(B_t\)。测试违反 \(r\) 是否会使原本优秀的响应变差。
  - **替代答案对**：从零开始独立生成**最强**的、满足 \(B_t\) 但**故意违反** \(r\) 的响应 \(b^−\)，再**最小化修复**成满足 \(r\) 的 \(b^+\)。测试是否存在与 \(r\) 无关但同样有效的解答路径，防止标准过于具体或仅关乎风格。
  - 所有响应在进入判断前需经评分标准感知的验证器校验，确保满足背景标准及指定的通过/未通过 \(r\) 条件。
- **评分标准盲门控机制**（表2）：
  - 成对判断器**仅接收查询和两个响应**，不知晓任何评分标准、标记或构建指令。每对按正序和反序各判断一次，归一化结果。
  - **接受条件**仅当：本地对结果显著偏向 \(a^+ (a^+ > a^-)\)，**且**替代对结果显著偏向 \(b^+ (b^+ > b^-)\)。否则基于具体模式拒绝（如 \(a^+ > a^-\) 但 \(b^+ ≈ b^-\) 判定为过于具体；双方均等同则判定为可选/风格化；任一对偏向违反方则判定为有害）。
- **树更新与记忆**：
  - 接受的 `ADD` 操作将候选标准作为新叶子加入；接受的 `SPLIT` 操作在子标准全部通过或通过“安全部分接受”规则（仅当被判为过于具体或可选时允许部分替换）时退役父标准并用通过的子标准替换。
  - 所有决策及其解释、反例均存入演化记忆，指导后续提议避免重复无效标准。

### 3. 实验设计
- **评估数据集与场景**：覆盖五个偏好基准套件的七个评估集：
  - JudgeBench
  - RM-Bench (Chat)
  - RewardBench (Chat 和 Chat-Hard)
  - RewardBench 2 (Precise-IF 和 Focus)
  - RubricBench (All)
- **基准与对比方法**：
  - **查询唯一基线**：Direct-Generate、TICK、RocketEval。均仅用查询生成评分标准，无需训练。
  - **训练的开源权重基线**：Rubric-RM-8B、Rubric-ARM-8B、Rubric-ARROW-8B。这些基线从外部偏好数据学习生成评分标准的专用模型。
- **统一评价设置**：
  - 所有 API 方法使用 Gemini 3.5 Flash 生成评分标准，所有方法用 Gemini 3.1 Pro 作为共享的验证器。
  - 评分标准转换为均匀得分，成对比较时若两响应评分相等则给予 0.5 平局分。
  - 提议者温度 0.7、替代对生成器温度 0.2，其余模块温度设为 0。
- **指标**：成对偏好判断准确率。

### 4. 资源与算力
- **算力说明**：本文框架**完全依赖 API 调用闭源 LLM**，不涉及任何模型训练或微调。因此文中未提及 GPU 型号、数量、训练时长等算力相关指标。框架的计算开销主要来自演化时构建响应对和成对比较的 API 调用次数。

### 5. 实验数量与充分性
- **实验数量**：主实验在七个评估集上给出了全面的准确率对比；表3展示了八种方法（含本文方法）的结果。
- **充分性与公平性**：
  - **公平性**：所有方法统一使用相同的生成和验证模型（Gemini 系列），消除了模型能力差异的干扰，对比相对公平。
  - **充分性不足**：作者在局限性中明确承认**未进行系统性的消融研究或敏感性分析**。例如，未单独量化本地对、替代对、树结构演化、演化记忆各自的贡献，也未分析演化预算、温度等超参数的影响。实验仅限于偏好基准，缺乏在下游强化学习任务上的验证及人工评估。
  - **结论支撑**：尽管主实验覆盖范围较广且表现优异，但由于缺乏消融和敏感性分析，内部机制的有效性证据仍需后续补充。

### 6. 论文的主要结论与发现
- **直接生成不可靠**：单次生成的评分标准即使看似合理，也因缺乏独立的质量信号而无法有效区分响应质量。
- **现有生成器缺乏验证环节**：无论是查询唯一方法还是基于训练的方法，虽能生成部分有用标准，但由于缺少候选级真伪检验，常混入过于具体或风格化的标准，导致在不同基准间性能波动大。
- **成对验证显著提升质量**：Rubrics on Trial 通过严格的双对验证，滤除了低价值、过于具体和风格化的标准，最终获得**最佳平均准确率（80.36%）**，并在七分之六的评估集上准确率最高，甚至超过了所有训练过的专用生成器。
- **框架有效性**：将抽象的标准集比较转化为具体的响应成对比较，是可行且高效的自我监督方式。

### 7. 优点
- **完全自主生成**：无需任何外部人工标注、响应偏好对或模型训练，仅凭查询即可演化出高质量评分标准集，极大降低了应用门槛。
- **创新的验证机制**：互补双对设计（本地编辑对 + 替代答案对）能够系统性地检验评分标准是否具有真正区分度、是否过多束缚解答方式或仅流于表面风格。
- **结构化演化**：树结构演化与记忆机制允许标准逐步增添和细化，并能复用失败反馈，避免重复提议无效标准。
- **任务通用性**：框架与具体任务弱耦合，只依赖标准判定的响应比较，理论上可应用于各类开放域生成任务。
- **性能突出**：在严格统一的评估条件下，以查询唯一方法之姿，超越了一系列使用外部偏好数据训练的专业化评分标准生成模型。

### 8. 不足与局限
- **下游任务验证缺失**：目前仅在偏好判断基准上验证了评分标准的区分能力，尚未证明这些标准作为细粒度奖励信号能在强化学习或策略优化中带来实际增益。
- **无消融分析**：缺乏对各个核心组件（本地对、替代对、树结构、记忆）以及超参数（如演化预算）的系统性定量分析，内部机制的重要性及最优配置不明。
- **依赖特定闭源模型**：实验仅使用了 Gemini 系列 LLM 作为生成器和判断器，框架在更换模型底座（如开源模型或更弱模型）时的鲁棒性未知。
- **合成响应的潜在偏差**：验证信号完全来自合成数据和 LLM 成对判断，响应生成中的偏见或判断噪声可能被引入评分标准集，缺乏与真实人类判决的对齐评估。
- **开销与可伸缩性**：每个候选标准需生成并判断四份响应，计算开销与演化步数成正比，在面对极大量查询或极度复杂任务时的效率受限。
- **局限性声明未覆盖的部分**：文中考虑的场景为单查询多步骤验证，对于动态对话、多轮交互等更复杂的上下文，评分标准的构建与验证机制可能需要较大调整。

（完）
