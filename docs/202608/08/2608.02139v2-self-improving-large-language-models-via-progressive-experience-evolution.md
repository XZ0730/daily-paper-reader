---
title: Self-Improving Large Language Models via Progressive Experience Evolution
title_zh: 通过渐进式经验演化实现自我改进的大型语言模型
authors: "Shijie Ren, Xiting Wang, Meng Li, Yujie Guo, Yunhang Yao, Ziheng Peng, Xunlong Wang, Yuetan Chen, Haoyang Zhou, Yunlong Liang, Fandong Meng"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.02139v2"
tags: ["query:self-evolve"]
score: 10.0
evidence: 提出通过渐进式经验精炼实现自我改进的LLM，能够将交互经验内化为模型能力。
tldr: 大型语言模型自我提升需将瞬时交互经验转化为持久模型能力，但现有范式失耦：测试时方法显式提取经验却无法内化至参数，训练时方法优化参数但缺乏经验积累机制。SPEE框架提出“经验蒸馏”，通过显式经验进化（从多次交互轨迹反思、提取、验证可迁移经验，利用全局经验池整合成败案例并过滤低效用）与特权引导自蒸馏内化，随后结合奖励驱动强化学习探索新策略。在5个数学推理基准上，3个模型规模的实验显示SPEE均显著超越测试时和训练时自进化基线。该研究桥接两类自我改进范式，验证了渐进式经验积累的有效性，为构建闭环自改进LLM提供新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.02139v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1657, \"height\": 865, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02139v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 867, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02139v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 828, \"height\": 566, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02139v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 863, \"height\": 559, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02139v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1582, \"height\": 728, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.02139v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1725, \"height\": 702, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02139v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1750, \"height\": 422, \"label\": \"Table\"}]"
motivation: 现有LLM自我改进中，测试时方法无法将经验内化为模型参数，训练时方法缺乏显式经验积累机制，亟需经验蒸馏环节。
method: SPEE先进行显式经验进化，通过全局经验池从多交互轨迹反思、提取并进化可迁移经验，再以特权引导自蒸馏内化，最后结合强化学习探索新策略。
result: 在5个数学推理基准上，SPEE在3个模型规模下均超越测试时和训练时自进化基线，验证了经验蒸馏的有效性。
conclusion: SPEE通过渐进式经验进化桥接两种范式，为LLM持续自进化提供了从经验提取到策略内化的完整框架。
---

## 摘要
具备自我改进能力的大型语言模型不仅需要有效的策略优化，还需要一种将短暂的交互经验转化为持久模型能力的原则性机制。现有的自我改进范式仍较为分散：测试时方法可以显式地提取经验，但无法将其内化到模型参数中；而训练时优化方法可以更新模型参数，但缺乏积累可迁移经验的显式机制。连接这两种范式需要一个尚待探索的关键中间阶段，即经验蒸馏。为解决这一问题，我们提出SPEE（Self-Progressive Experience Evolution，自我渐进式经验演化），一个统一的训练后框架，该框架依次执行显式经验演化和随后的隐式策略优化。在显式经验演化阶段，SPEE对多轮交互收集的轨迹进行反思，以提取、验证并逐步演化可迁移的经验，随后通过特权引导的在策略自蒸馏（OPSD）将其内化到策略中。在隐式策略优化阶段，奖励驱动的强化学习利用这些内化的先验知识探索新的解题策略。在经验演化阶段，一个持续演进的全局经验池整合成功和失败轨迹中的知识，滤除低效用经验，并缓解由单个轨迹引起的事后合理化。在五个数学推理基准上的实验表明，SPEE在三种模型规模下均一致优于测试时和训练时的自我演化基线。源代码可在https://github.com/rrrsj/SPEE获取。

## Abstract
Large language models (LLMs) capable of self-improvement require not only effective policy optimization, but also a principled mechanism for transforming transient interaction experience into persistent model capabilities. Existing self-improvement paradigms remain fragmented: test-time methods can explicitly extract experience but cannot internalize it into model parameters, whereas training-time optimization methods can update model parameters but lack an explicit mechanism for accumulating transferable experience. Bridging these two paradigms requires a critical intermediate stage that remains underexplored, namely \emph{experience distillation}. To address this gap, we propose \textbf{SPEE} (\textbf{S}elf-\textbf{P}rogressive \textbf{E}xperience \textbf{E}volution), a unified post-training framework that sequentially performs explicit experience evolution followed by implicit policy optimization. During explicit experience evolution, SPEE reflects on trajectories collected from multiple interactions to extract, verify, and progressively evolve transferable experience, which is subsequently internalized into the policy through privilege-guided On-Policy Self-Distillation (OPSD). During implicit policy optimization, reward-driven reinforcement learning leverages these internalized priors to explore novel solution strategies. In the experience evolution stage, a continuously evolving global experience pool consolidates knowledge from both successful and failed trajectories, filters out low-utility experience, and mitigates post-hoc rationalization induced by individual trajectories. Experiments on five mathematical reasoning benchmarks demonstrate that SPEE consistently outperforms both test-time and training-time self-evolution baselines across three model scales. The source code is available at https://github.com/rrrsj/SPEE.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：
  - 当前大型语言模型（LLM）的自我进化主要分为两类范式：
    - **测试时方法**（如上下文学习、检索增强）可以显式提取并利用经验，但经验始终留在模型参数之外，无法内化为持久能力。
    - **训练时方法**（如强化学习、RL）通过奖励信号更新参数，但轨迹中的经验仅以稀疏奖励的形式间接影响模型，缺乏对成功与失败模式的有效显式积累。
  - 两类方法之间的**缺失环节**是**经验蒸馏**：如何将交互轨迹中瞬时的、实例相关的信息，压缩成可迁移、可积累的稳定经验并注入模型参数，然后在此基础上进行策略优化。

- **整体含义**：
  - 本文提出 **SPEE（Self-Progressive Experience Evolution）**，一个统一的训练后框架，通过“显式经验演化 → 经验内化 → 隐式策略优化”的闭环，将短暂的交互经验转化为持久模型能力，从而桥接测试时与训练时的自我进化范式。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

- **核心思想**：
  - 从**经验为中心**的视角定义自我进化：从轨迹 \(T\) 中提取可迁移的紧凑经验 \(E\)，使其保留与未来能力 \(C\) 互信息（\(I(E;C)\)）最大的部分，同时减少与轨迹无关的冗余（最小化 \(I(T;E)\)）。
  - 将经验蒸馏定义为 \(\pi_{\theta'}(y|q) \leftarrow \pi_{\theta}(y|q, e)\)，即让模型在看不到经验时也能表现出经验指导下的行为。
  - SPEE 通过**全局经验池**的渐进演化，将成功与失败轨迹中的推理策略、约束条件、常见错误等抽象成高质量经验，再以特权自蒸馏的方式注入策略，随后用 RL 进行进一步探索。

- **关键技术细节与流程（分两个阶段）**：

  **Stage I：显式经验演化**
  - 在每轮演化 \(k\) 中，当前策略 \(\pi_{\theta_k}\) 对每个问题采样 \(G\) 条回答，依据二元奖励分为成功集 \(T^+\) 和失败集 \(T^-\)。
  - 随机选择 \(S\) 条轨迹，用策略本身作为经验提取器 \(\Sigma_{\theta_k}\)，生成候选经验项 \(e_i^{(s)}\)（正/负经验）。
  - 将候选经验 \(E^{(k)}_{new}\) 与全局经验池 \(E^{(k)}\) 通过演化算子 \(\Phi\) 合并、去重、抽象化，得到临时池 \(\hat{E}^{(k+1)}\)。
  - 在验证探针集 \(Q_{pb}\) 上评估每个候选经验对策略期望奖励的边际效用 \(w^{(k)}(e)\)，过滤掉低效用项，更新全局经验池 \(E^{(k+1)}\)。
  - 经验蒸馏：训练时，教师分支接收问题+经验 \(e\)，学生分支仅接收问题，通过**特权引导的在策略自蒸馏（OPSD）** 最小化学生与教师分布的反向 KL 散度：
    \[ \mathcal{L}_{\text{OPSD}}(\theta) = \mathbb{E}_{q\sim D}\left[ D_{\text{KL}}\left( \pi_{\theta}^{\text{stu}}(\cdot|q) \parallel \pi_{\bar{\theta}}^{\text{tea}}(\cdot|q) \right) \right] \]
    其中教师分布为 \(\pi_{\bar{\theta}}(y'|q, e, y_{<t})\)，通过学生采样的轨迹进行在策略重打分。

  **Stage II：隐式策略优化**
  - 在蒸馏后的策略上应用 **GRPO**（Group Relative Policy Optimization）：
    - 对每个问题采样 \(G\) 个回答，计算组内奖励均值 \(\mu_G\)、方差 \(\sigma_G^2\)，得到相对优势 \(A_i = \frac{r_i - \mu_G}{\sigma_G + \delta}\)。
    - 使用 PPO 风格的裁剪重要性比和优势，最大化目标 \(J_{\text{GRPO}}(\theta)\)。
  - 蒸馏初始化使策略更倾向于高奖励区域，提高 GRPO 探索效率。

- **整体流程图示**：全局经验池通过多轮交互不断进化，蒸馏后的策略参与新的轨迹采样，形成闭环自改进。

### 3. 实验设计：数据集、基准、对比方法

- **模型与训练数据**：
  - 基座模型：Qwen3‑1.7B‑base、4B‑base、8B‑base。
  - 训练数据：DAPO‑math‑17k（含数学推理问题）。

- **评估基准**：
  - **AIME 2024** 和 **AIME 2025**（竞赛级数学题，报告 pass@16 平均准确率）。
  - **GSM8K**、**MATH500**、**Minerva Math**（分别对应小学数学、综合数学推理、数量推理，报告 pass@1 准确率）。

- **对比方法**：
  - **Base**：原始基座模型，不做任何后训练。
  - **Domain Prompt**：测试时手工设计的数学推理提示词。
  - **GRPO**：直接对基座模型进行组相对策略优化。
  - **SDPO**：基于自蒸馏的方法，直接蒸馏完整参考轨迹。
  - **SPEE (Ours)**：本文提出的完整框架。

- **消融实验**：
  - 移除 Stage II（仅保留经验演化与蒸馏）。
  - 移除共享经验池（不使用全局池，仅使用实例级轨迹）。
  - 控制训练步数与采样 rollout 总数，保证对比公平。

- **训练动态与敏感性分析**：
  - 全错组比例与策略熵的变化（Stage I 提升有效采样率同时保持多样性）。
  - 数据效率对比（SPEE 以更少训练轨迹达到同等性能）。
  - 经验演化轮数的影响（0~4 轮对采样准确率的提升）。

- **定性分析**：通过具体数学题展示经验如何引导模型采用更结构化、系统化的推理步骤，而非记忆答案。

### 4. 资源与算力

- **文中未明确说明**所使用的 GPU 型号、数量、训练时长、批次大小等具体算力资源信息。
- 仅指出控制了不同方法间的训练步数和总采样 rollout 数量，以保障公平对比。若需精确复现，需参考其开源代码库。

### 5. 实验数量与充分性

- **实验数量丰富**：
  - 主要性能对比：3 个模型规格 × 5 个数学基准 × 4 种基线 + 本文方法（共约 75 组对比结果，表 1）。
  - 消融实验：2 个模型规格 × 3 种变体（共 6 组对比，表 2）。
  - 训练动态实验：全错组比例与熵分析（图 2）、数据效率曲线（图 3）、多轮演化效果（图 4）。
  - 定性示例（图 5）。
- **充分性与客观性**：
  - 覆盖了三个不同规模的模型，确保结论的尺度稳定性。
  - 基准涵盖竞赛级、中小学级和综合推理，代表性较强。
  - 对比基线包含了测试时提示、纯 RL 训练、自蒸馏方法，覆盖了当前主要自我进化范式。
  - 消融实验与动态分析有效验证了各组件的作用。
  - 实验设计上控制了训练数据的总量（DAPO‑math‑17k）和采样预算，体现了公平比较。

### 6. 论文的主要结论与发现

- SPEE 在所有三个模型规模、五个数学基准上均**一致且显著地优于**所有测试时与训练时基线：qwen3‑1.7B 平均提升 +4.87%，4B 提升 +6.96%，8B 提升 +6.53%。
- Stage I（经验演化与蒸馏）能**显著减少全错响应组比例**，为 GRPO 提供更具信息量的梯度信号，同时不提前塌缩策略熵。
- SPEE 具有**更高的数据效率**：在达到同等性能时可节省约 28% 的训练轨迹。
- 全球经验池是有效组件：移除后性能大幅下降，验证了其对跨问题可迁移知识的积累和过滤作用。
- 多轮经验演化可**持续提升**策略的采样准确率（4 轮相对提升约 20.34%），表明经验质量可以与策略能力共同进化。
- 显式提取的抽象经验（如“分解问题步骤”、“系统枚举”）帮助模型学习通用的推理策略，而非简单记忆答案或路径。

### 7. 优点：方法或实验设计上的亮点

- **范式创新**：首次明确提出“经验蒸馏”作为桥接测试时与训练时自我进化的关键中间阶段，并给出系统框架。
- **经验表示设计**：用可演化的全局经验池替代原始轨迹，过滤低效用条目，缓解事后合理化，使经验更具**压缩性和可迁移性**。
- **双层优化机制**：先通过密集监督的 OPSD 将经验内化，再结合 RL 进行奖励驱动的探索，改善了 RL 对初始模型质量的依赖，提高了采样效率。
- **实验详尽**：多尺度、多基准、多基线对比，配合消融实验和动态分析，清晰展示了各组件的贡献和方法的有效性。
- **闭环设计**：经验池随着策略增强而不断进化，体现了自我改进的持续性。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **领域范围单一**：仅在数学推理领域进行了验证。对于代码生成、智能体交互等更复杂、奖励信号更稀疏或不可靠的任务能否直接迁移尚未知。
- **计算开销不透明**：文中未提及经验提取、池进化、多轮演化的具体算力成本（例如每轮采样数、验证探针集大小等），可能带来较大的计算开销。
- **超参数多且敏感**：经验池的更新策略、效用阈值 \(ϵ\)、探针集选择、教师‑学生蒸馏的细节配置等可能对性能有较大影响，但未进行全面的超参数敏感性分析。
- **RL 算法单一**：Stage II 仅使用了标准 GRPO，未探索与其他在线 RL 变体的结合是否能进一步增益。
- **多轮演化的实际收益未在最终结果中体现**：论文主要结果仅使用单一轮经验演化（可能出于效率考虑），而多轮演化带来的采样准确率提升并未转化为最终基准测试上的额外收益数据，其实际价值有待验证。
- **基线和数据集的局限性**：SDPO 基线的配置细节不详；仅在 Qwen3 系列上实验，未在 LLaMA 等其他主流模型上验证通用性。

（完）
